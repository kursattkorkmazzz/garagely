// Re-export generic list components with Settings-specific names for backwards compatibility
export {
  AppListSection as AppSettingsSection,
  AppListItem as AppSettingsItem,
} from "@/components/ui/app-list";
